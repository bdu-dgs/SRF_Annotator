import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#1f6f8b"
    },
    secondary: {
      main: "#b65f2a"
    },
    background: {
      default: "#f7f9fb"
    }
  },
  shape: {
    borderRadius: 6
  }
});

